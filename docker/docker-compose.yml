# Use postgres/example user/password credentials
version: '3.1'

services:

  postgre:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: example

  redis:
    image: redis
    restart: always
